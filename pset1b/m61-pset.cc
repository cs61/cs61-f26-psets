#include "m61.hh"

void* m61_malloc(size_t) {
    assert(false && "Pset not started yet");
}

void m61_free(void*) {
    assert(false && "Pset not started yet");
}

m61_statistics m61_get_statistics() {
    assert(false && "Pset not started yet");
}
